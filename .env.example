# Copy this file to .env if you want to override the Docker base image.
# The default avoids Docker Hub auth.docker.io by using AWS ECR Public.
NODE_IMAGE=public.ecr.aws/docker/library/node:22-alpine

# Alternative: replace this with another registry mirror reachable from your network.
# NODE_IMAGE=<your-registry-mirror>/library/node:22-alpine
