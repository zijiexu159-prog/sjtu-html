# Copy this file to .env if Docker Hub is slow or unreachable.
# Keep the default official image when Docker Hub is accessible.
NODE_IMAGE=node:24-alpine

# Example: replace this with a registry mirror that is reachable from your network.
# NODE_IMAGE=<your-registry-mirror>/library/node:24-alpine
